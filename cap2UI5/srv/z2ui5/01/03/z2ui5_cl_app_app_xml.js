
class z2ui5_cl_app_app_xml {
  static get() {
    let result = ``;
    result = `<mvc:View controllerName="z2ui5.controller.App"` + ` xmlns:mvc="sap.ui.core.mvc" displayBlock="true"` + ` xmlns="sap.m">` + ` <App id="app">` + ` </App>` + `</mvc:View>` + `` + ``;
    return result;
  }
}

module.exports = z2ui5_cl_app_app_xml;

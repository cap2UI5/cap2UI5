{
  "name": "cap2ui5-dev",
  "version": "1.0.0",
  "private": true,
  "description": "Development tooling for cap2UI5 — the deployable CAP project lives in cap2UI5/",
  "license": "MIT",
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch",
    "transpile": "node scripts/abap2js.js",
    "mirror_frontend": "node scripts/mirror-frontend.js",
    "mirror_input": "node scripts/mirror-input.js",
    "sync_backend": "node scripts/sync-backend.js"
  },
  "devDependencies": {
    "@abaplint/core": "^2.119.56",
    "jest": "^30.2.0"
  }
}
